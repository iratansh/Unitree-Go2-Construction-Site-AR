name: go2-sim
channels:
  - defaults
dependencies:
  - python=3.9
  - pip
  - pip:
    - pybullet
    - numpy
    - pandas
    - matplotlib
    - opencv-python
    - cyclonedx==0.10.2
    - rich-click